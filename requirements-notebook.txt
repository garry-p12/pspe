# Pip-only, fully pinned install for Colab / Kaggle notebooks.
#
# Colab and Kaggle have no first-class conda support, so `make setup` (which
# builds a conda env) does not apply there. Use this file instead:
#
#     !pip install -q -r requirements-notebook.txt
#     !pip install -q -e .
#
# Torch is deliberately NOT pinned here: both platforms ship a CUDA-matched
# torch build preinstalled, and pinning a version would force a multi-GB
# reinstall against the wrong CUDA runtime. Check what is already there with
# `import torch; torch.__version__` before overriding anything.
#
# Versions below are the ones this repo was developed and tested against.

numpy==2.4.6
scipy==1.17.1
gymnasium==1.3.0
hydra-core==1.3.4
omegaconf==2.3.1
tensorboard==2.21.0
matplotlib==3.11.1
pandas==3.0.5
rich==15.0.0
pytest==9.1.1
pytest-timeout==2.4.0

# Phase 3 / Phase 4 open-weight backbones.
transformers==5.14.1
peft==0.19.1
accelerate==1.14.0
pillow==12.3.0

# Real perception data: EuroSAT (Sentinel-2) from the HuggingFace Hub.
datasets>=3.0

# CUDA-only; Colab and Kaggle both provide a GPU, so 4-bit quantisation works
# there even though it cannot be installed on macOS/arm64.
bitsandbytes==0.48.2
